// I DESCEND

// I descend into the shadow of my innerHTML
// Cascading sheets illuminate privacy's penumbra 

let privacy = document.getElementById("protected-class");

// Which class?
// How to id? To id one element might exclude all others
// Amendments alter a body's constitution
// But to what extent?
// Who finds sanctuary?

let count = 0;

window.addEventListener("click", theBillofRights)

// To enumerate or not, 
// what

function theBillofRights() {

// extends
// And beyond that
// Who can

	count++

// Who can 

	console.log(count)

// What

	if (privacy.innerHTML === "cast into darkness fundamental rights illuminated in the Constitution's penumbra") {

// Be warned if

        privacy.innerHTML = "THE SHADOW DOCKET";

// And then

		privacy.classList.add("impunity");

// Once elucidating protection, three new fates shatter protections

    } else if (count >=3) {

// Trump justice
// Justices 
// Blind justice 

		privacy.innerHTML = "cast into darkness fundamental rights illuminated in the Constitution's penumbra";
		count = 0;

		if (privacy.classList.contains("impunity")) {
			privacy.classList.remove("impunity");
		}
	} else {

        privacy.innerHTML = "unsigned majority opinions obscure accountability";
		if (privacy.classList.contains("impunity")) {
			privacy.classList.remove("impunity");
		}
	}
}

// Unprecedented challenges precedents
// Unprecedent stare decisis
// To stare, but not to see 
// To bury what was once found

// Res judicata 
// Erased

// The Court Wades like Peter Pan chasing shadows
// To rejoin one's ever-present but elusive silhouette
// One's essential self
// The Court Rows
// It rocks Roe 
// The innerHTML finds sanctuary at Home 